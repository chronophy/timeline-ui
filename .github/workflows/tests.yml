
on:
  workflow_call:

jobs:
  tests:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7

      - uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Check formatting
        run: mise run lint

      - name: Unit Tests
        run: mise run tests
